import 'package:flutter/material.dart';

class PredicitonHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
        appBar: AppBar(
          elevation: 0,
        ),
        body: Center(
          child: Container(
            child: Text("WIP PLEASE KEEP CALM!",style: TextStyle(fontSize: 32),),
          ),
        ));
  }
}
